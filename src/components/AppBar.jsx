import React from 'react';
import PropTypes from 'prop-types';

import './AppBar.css';

const AppBar = (props) => {
	return (
		<nav className="AppBar">
			Proxiad
		</nav>
	);
};

AppBar.propTypes = {};

export default AppBar;