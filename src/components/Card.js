import React from 'react';
import './Card.css';

const Card = () => (
    <section class="card person-card">
    <div class="card-content">
          _text_
    </div>
  </section>
);

export default Card;