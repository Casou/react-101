import React from 'react';
import './Card.css';

const Card = () => (
    <section className="card person-card">
    <div className="card-content">
          _text_
    </div>
  </section>
);

export default Card;